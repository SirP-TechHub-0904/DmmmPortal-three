@model IEnumerable<DmmmPortal.Models.Entities.ImageSlider>


@foreach (var image in Model)
{
    if (image.Content != null)
    {

        <div>
            <img data-u="image" src="data:image/jpg;base64,@(Convert.ToBase64String(image.Content))" alt="dmmm ahiaeke" />

        </div>
    }
}